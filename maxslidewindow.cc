#include "utils.h"

using Pair = pair<int, int>;

int maxSlideWindow(vector<int> &nums, int k) {
    // 1. brute force
    // 2. max heap
    // 3. deque

    return 0;
}

int main(int argc, char *argv[]) { return 0; }
